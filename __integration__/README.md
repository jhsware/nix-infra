# Integration Tests

In order to run integration tests you need to set up a .env file.

# provisioning

This tests uses an existing server on Hetzner Cloud to test converting different
base OS distros to NixOS. The tests are written in BASH and results are output to
stdout. You can copy the results to an LLM for convenient analysis.
