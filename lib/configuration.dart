import 'dart:io';
// import 'package:nix_infra/helpers.dart';

Future<void> copyConfigurationTemplates(
  Directory workingDir, {
  required String template,
}) async {

  // mkdir(workingDir.path,
  //     names: ['app_modules', 'modules', 'node_types', 'nodes', 'openssl']);
}
